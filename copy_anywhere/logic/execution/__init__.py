"""The format-2 stage evaluator.

`context` holds one operation's shared state, `facades` the immutable note and card views
code mode sees, `actions` the leaf stage handlers, `evaluator` the block dispatch, and
`commit` what happens to the mutations a run produced.
"""
