# What the test suites need, for every addon in the repo. The versions are the ones the
# full run was last verified against; bump them together and rerun `python -m pytest`.
#
# Install into the interpreter the tests run on -- the system Python 3.10 here, the same one
# .vscode/settings.json and pyrightconfig.json point at, and the one to run `python -m mypy`
# with: mypy reads anki/aqt out of whichever interpreter it runs on. The repo's .venv is a
# different interpreter holding japanese_note_ai_ops' runtime libraries and has no Anki.
#
#   python -m pip install -r requirements-dev.txt
#   python -m pip install --no-deps -r requirements-dev-nodeps.txt
#
# The second line is pytest-anki2, which cannot go in this file: see the reason there.

# The real `anki` package backs the default (real-collection) mode; `aqt` pulls in PyQt6
# and QtWebEngine. Qt is named here directly rather than through one of aqt's extras, so a
# bump of either is a deliberate edit to this file; aqt 26.9.2's `qt` extra pins the same
# PyQt6 and PyQt6-WebEngine as below.
anki==26.9.2
aqt==26.9.2
PyQt6==6.11.0
PyQt6-WebEngine==6.11.0

pytest==8.3.3
# Provides `qtbot` to pytest-anki, and reads `qt_api = pyqt6` from pytest.ini.
pytest-qt==4.5.0
